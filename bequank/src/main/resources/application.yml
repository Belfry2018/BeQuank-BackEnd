spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bequank?characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: nibabaqyc
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    database: mysql
    show-sql: false
    hibernate:
      ddl-auto: update
      naming:
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyJpaCompliantImpl
  cache:
    type: redis
  redis:
    host: 127.0.0.1
    port: 6379
    password:
    jedis:
      pool:
        max-idle: 0
        max-active: 0

belfry:
  expiration_time: 10800000
  secret: "belfry2018"
  token_prefix: "Bearer "
  header_string: "Authorization"
  cache_name: "loginList"
  key_prefix: "loginList::"
server:
  port: 9000
